import { RefElDirective } from './ref-el.directive';

describe('RefElDirective', () => {
  it('should create an instance', () => {
    const directive = new RefElDirective();
    expect(directive).toBeTruthy();
  });
});
